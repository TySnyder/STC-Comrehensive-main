node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Live client data — never commit (PHI risk)
live data/
*.xlsx
*.xls
*.csv

# Design reference exports (Stitch, etc.) — scratch input, not source
.stitch/
